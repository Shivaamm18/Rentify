{
  "name": "rentify",
  "version": "1.0.0",
  "description": "A rental property platform for India",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm install && CI=false npm run build && rm -rf ../build && mv build ../build"
  },
  "keywords": [
    "rental",
    "property",
    "real-estate",
    "india"
  ],
  "author": "Rentify",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}